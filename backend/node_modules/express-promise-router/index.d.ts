declare module 'express-promise-router' {
    import { Router, RouterOptions } from 'express-promise-router/node_modules/express';

    function PromiseRouter(options?: RouterOptions): Router;

    export default PromiseRouter;
}
